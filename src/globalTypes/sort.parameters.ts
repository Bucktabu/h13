export enum SortParameters {
  Id = 'id',
  Name = 'name',
  YoutubeUrl = 'youtubeUrl',
  CreatedAt = 'createdAt',
  Title = 'title',
  Content = 'content',
  BlogId = 'blogId',
  BlogName = 'blogName',
  Login = 'login',
  email = 'email',
}

export enum SortDirection {
  Ascending = 'asc',
  Distending = 'desc',
}
